services:
  sz2kindle:
    image: ghcr.io/valentinlehmann/sz2kindle:main
    restart: unless-stopped
    environment:
      - TZ=Europe/Berlin
      # Cron expression — default: every day at 06:00.
      - SZ2KINDLE_CRON=0 6 * * *
    volumes:
      # Mount config and persistent data (session, sent tracking).
      - ./config.ini:/data/config.ini:ro
      - sz2kindle-data:/data

volumes:
  sz2kindle-data:
